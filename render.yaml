# Render.com Blueprint — Sarkari Hai
services:
  - type: web
    name: sarkari-hai
    runtime: node
    plan: free
    buildCommand: node src/build.js
    startCommand: node src/server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20
